type InputMap = {
  wPressed: boolean;
  aPressed: boolean;
  sPressed: boolean;
  dPressed: boolean;
  onePressed: boolean;
  twoPressed: boolean;
  shiftPressed: boolean;
  spacePressed: boolean;
};

export default InputMap;
